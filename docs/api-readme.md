# rnp-wasm API reference

This is the auto-generated API reference. For a tutorial-style walkthrough, see [api.md](api.md).

The reference is generated by TypeDoc from TSDoc comments in `ts/`. To rebuild locally:

```sh
npm run docs:build
npm run docs:preview
```
